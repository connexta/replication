query GetReplications {
    replication {
        replications {
            name
            id
            filter
            biDirectional
            suspended
            version
        }
    }
}