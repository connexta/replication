mutation updateRepsync($id: Pid!, $name: String!, $sourceId: Pid!, $destinationId: Pid!, $filter: CqlExpression!){
  updateRepsync(id: $id, name: $name, sourceId: $sourceId, destinationId: $destinationId, filter: $filter){
    id
    name
    source{
      id
      name
      address{
        host{
          hostname
          port
        }
        url
      }
    }
    destination{
      id
      name
      address{
        host{
          hostname
          port
        }
        url
      }
    }
    filter
  }
}
